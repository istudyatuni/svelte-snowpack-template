{
  "scripts": {
    "dev": "snowpack dev",
    "build": "snowpack build",
    "format": "prettier --write --plugin-search-dir=. './**/*.js' './**/*.svelte' './**/*.css' './*.mjs' './**/*.json'",
    "format:check": "prettier --check --plugin-search-dir=. './**/*.js' './**/*.svelte' './**/*.css' './*.mjs' './**/*.json'",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "@snowpack/plugin-svelte": "^3.7.0",
    "prettier": "^2.4.1",
    "prettier-plugin-svelte": "^2.4.0",
    "snowpack": "^3.3.7",
    "svelte": "^3.44.1"
  }
}
